# Glossary

## Environment Variable (env var)
A named value available to programs at runtime.

Example:
```bash
export OSB_HOME="/home/kingb/.openclaw/workspace/opensitebackup"
```

Programs/scripts read these variables to know where to store files, connect to hosts, and choose behavior.

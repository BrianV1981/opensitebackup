# OpenSiteBackup Marketing Site (Node.js)

Run locally:

```bash
cd site
npm run dev
```

Default URL:

- http://localhost:4173

This site is static HTML/CSS served by a tiny Node HTTP server (`server.mjs`) with no runtime dependencies.
